#include "data_generator.h"
// this may only used by msgPack

void DataGenerator::fillString(testData& data, size_t nkeys, size_t svalMin, size_t svalMax, const std::vector<std::string> &values) {
    
    data.qieezdij = values[0];
    data.rbqjhkqq = values[1];
    data.tnxueiff = values[2];
    data.wfzxitxa = values[3];
    data.wjhiporp = values[4];
    data.bntuygoi = values[5];
    data.smxkhuxz = values[6];
    data.jjomypit = values[7];
}
