#include "data_generator.h"

// this may only used by msgPack
void DataGenerator::fillString(testData& data, size_t nkeys, size_t svalMin, size_t svalMax, const std::vector<std::string> &values) {
    data.hoaqrzyx = values[0];
    data.ofshdgxa = values[1];
    data.kaclagyq = values[2];
    data.dftwwleq = values[3];
    data.syuuwran = values[4];
    data.zcmcvlcr = values[5];
    data.fzqhviby = values[6];
    data.oedhxeco = values[7];
}
